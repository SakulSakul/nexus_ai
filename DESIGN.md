# DESIGN.md — DF COMPASS

> 신세계디에프 사내 컴플라이언스 RAG 어시스턴트. 임직원이 사규(윤리·신고·안전·
> 정보보안·재무·공정거래 등)를 확인하는 도구.
> **Claude Code/AI 어시스턴트는 모든 UI 작업 전 이 문서를 읽고 단일 기준으로 따른다.**
> 플랫폼: **Streamlit** — 마크업 직접 작성 불가, Streamlit DOM 위에 CSS 주입만 가능.

## 0. 디자인 원칙 (제품 정체성)
- **신뢰·공식·차분·가독.** 컴플라이언스 도구는 읽기 쉽고 권위 있어 보여야 한다.
- **라이트 테마 고정.** 다크/그래디언트/개발툴룩 금지 — 장문 규정 가독성·신뢰감 저하.
- 신세계 시그니처 레드는 **액센트 전용**. 면적 채색 금지.
- 화려함보다 여백·정렬·일관성. 절제가 기본값.

## 1. 색 토큰 (app.py `:root` — 신규 색 만들지 말고 이 변수 재사용)
| 토큰 | 값 | 용도 |
|---|---|---|
| `--c-primary` | `#1A1A1A` | 본문/구조 무채색 |
| `--c-accent` | `#C8102E` | 신세계 시그니처 레드 — **액센트 전용** |
| `--c-accent-dark` | `#9A0C24` | 호버 |
| `--c-accent-bg` | `#FCEBEE` | 옅은 핑크 — 배너/하이라이트 배경 |
| `--c-text` | `#333333` | 본문 |
| `--c-caption` | `#767676` | 캡션/메타 |
| `--c-muted` | `#AEAEAE` | 비활성/보조 |
| `--c-border` | `#E0E0E0` | 경계선 |
| `--c-surface` | `#F7F7F7` | 카드/표면 |
| `--c-bg` | `#FFFFFF` | 배경 |

## 2. 타이포그래피
- 폰트: `var(--font)` = `Pretendard, -apple-system, 'Apple SD Gothic Neo', 'Noto Sans KR', sans-serif`.
- **답변 본문 정규화 (PR-UI1):** chat message 내 `p/li` = 0.95rem / line-height 1.72;
  `h1~h5` = 1.03rem / 700 / margin 0.95em 0 0.35em; 리스트 간격 통일.
- 메타데이터(⏱️ 시간·🤖 모델·출처)는 작게(13px) `--c-caption`, 본문과 border-top로 분리.

## 3. 레이아웃 / 간격
- `layout="wide"` (대시보드·도서관 탭용). **단 답변은 폭 제한 (PR-UI2):**
  `stChatMessage` max-width 880px, `.stMarkdown` 840px → 한 줄 ~40~50 한글자(가독 폭).
- 답변 카드 / 입력창 사이 충분한 여백. 섹션 간 세로 리듬 확보.

## 4. 컴포넌트
### 채팅 아바타 (PR-UI3)
- 봇 = `🧭` (브랜드 COMPASS), 유저 = `👤`. **모든 chat_message에 일관 지정.**
- 기본 Streamlit 색박스 금지 → 뉴트럴 디스크(`#f1f3f5` bg, `#e5e8eb` border).
### 답변 본문 구조 (synthesizer 출력 규약 — 항상 같은 순서·마크업)
1. 질문 확인 → 2. 핵심 결론(1문장, 핵심어 볼드) → 3. `📋 사규 기준` → 4. `⚖ 징계 기준`
→ 5. 권장 행동(필요 시 3단계 이내) → 6. `[참조: …]` → 7. 카테고리 chip + 신뢰도 chip + 메타.
### 인용 (Citation)
- 인라인: `📎` + **볼드 사규명**. 하단 `[참조: …]` 블록은 contexts 기준 정확 형태.
- **조항 번호는 청크 헤더에 실제 존재할 때만.** 없는 조항번호 생성 = hallucination, 절대 금지.
### 징계 수위 색 (의미 색)
- 경징계(경고장·서면경고·견책): 점강. 중징계(정직·강격·징계해직): `--c-accent` 강조.
### Chips (카테고리 / 신뢰도)
- 카테고리 chip + 신뢰도 chip은 답변 본문 직후, 메타 위. 작고 차분하게.
### 후속질문 카드 (PR-UI1 grounded)
- 출처 = 검색 최상위 문서의 `auto_query_examples` (grounded·답변 보장).
- **LLM 자유생성 후속질문/자유서술 역질문 금지.**

## 5. 인터랙션
- 스트리밍: `st.write_stream` native append(단어 단위). 장시간 로딩 스피너 금지.
- 클릭형 칩/후속질문: `clicked_q` → `st.rerun()` 패턴 재사용(새 배선 만들지 말 것).
- 피드백 👍/👎, 액션(📞 인사교육팀 / 🔄 다시 답변)은 답변 하단 일관 배치.

## 6. Streamlit 제약 (반드시 인지)
- 컴포넌트 마크업 직접 작성 불가 → **CSS 주입만.** `data-testid`(stChatMessage 등)는
  버전 의존 → 깨지면 graceful no-op, 셀렉터 1줄만 조정(앱은 안 깨짐).
- 전역 `<style>`는 app.py 상단 단일 블록. 새 규칙은 그 안에, 기존 `var(--c-*)` 재사용.
- **불변:** `core/` 식별자(`nexus_*`), `NEXUS_*` env, DB 테이블명. UI 표면과 내부 코드명은 분리.
- HR 부서명은 항상 `인사교육팀` (절대 `인사팀` 아님).

## 7. 하지 말 것 (Anti-patterns)
- 다크/그래디언트/개발툴 테마 도입
- 답변 full-width(줄 길이 폭주)
- 액센트 레드 면적 채색
- 기본 Streamlit 아바타 색박스 방치
- 조항번호 hallucination
- 이모지 남발(아바타·섹션 마커·의미 색 외)
- 한 번에 한 트리거씩 땜질(whack-a-mole) — 결함은 클래스 단위로 한 PR에 처리
